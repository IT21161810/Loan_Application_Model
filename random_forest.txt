 
Random Forest model was trained to perform classification using a dataset split into 72% training data, 18% validation data, and 10% test data. The Random Forest algorithm, known for building an ensemble of decision trees to improve prediction accuracy and reduce overfitting, demonstrated strong performance. It achieved a training accuracy of 95%, indicating effective learning on the training set. The validation accuracy was 80%, showing that the model generalizes well, though a slight drop from training accuracy suggests mild overfitting. The test accuracy of approximately 79% closely aligns with the validation score, confirming that the model maintains stable performance on completely unseen data. These results highlight that the Random Forest model is well-optimized and performs reliably across different data splits.
 
The Test Classification Report presents a detailed performance summary of a binary classification model. For class 0, the model achieved a precision of 0.71, recall of 0.53, and an F1-score of 0.61, indicating moderate performance with some difficulty in correctly identifying all true instances of class 0. For class 1, the model performed significantly better, with a precision of 0.81, a high recall of 0.91, and a strong F1-score of 0.86, showing that it is effective at detecting positive cases. The overall accuracy of the model on the test set is 0.79 (79%). The macro average scores, which give equal weight to each class, show slightly lower recall and F1-score due to the imbalance in performance across classes. The weighted averages, which account for class support, reflect the dominance of class 1 and provide an F1-score of 0.78, reinforcing the model’s overall strong performance despite class imbalance.

 
The Validation Confusion Matrix provides insight into the classification performance of the model by comparing predicted labels against actual outcomes. It shows that the model correctly predicted 10 "Rejected" cases and 39  "Approved" cases. However, it misclassified 9 "Rejected" instances as "Approved" (false positives), and 4 "Approved" instances as "Rejected" (false negatives). This suggests the model is particularly strong at identifying "Approved" cases, but struggles more with correctly identifying "Rejected" cases, which may indicate a slight bias toward predicting the majority class.
 
The image displays a Validation ROC (Receiver Operating Characteristic) Curve used to evaluate the performance of a binary classification model. The curve plots the True Positive Rate (Sensitivity) against the False Positive Rate, illustrating the trade-off between the two. The orange line represents the ROC curve for the model, while the diagonal dashed blue line indicates a random classifier (AUC = 0.5). The model's Area Under the Curve (AUC) is 0.73, suggesting it has good discriminatory power and performs significantly better than random guessing.
 
 
